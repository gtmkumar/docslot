{
  "ConnectionStrings": {
    "_comment": "Host/Database/Username shown here for visibility only (no password — Npgsql would refuse to connect with just this). The actual Password comes from `dotnet user-secrets` (mediq.Api and mediq.AppHost both set ConnectionStrings:platform-db), which loads AFTER this file and overrides the whole value at runtime. Never put the password here — this file is committed to git.",
    "platform-db": "Host=187.127.154.205;Port=5432;Database=docslot_platform_dev;Username=postgres;password=postgresql;SSL Mode=Prefer;Trust Server Certificate=true"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "WhatsApp": {
    "VerifyToken": "docslot-verify",
    "AppSecret": "docslot-app-secret",
    "PhoneNumberIdToTenant": {
      "PNID_APOLLO": "11111111-1111-1111-1111-111111111111"
    }
  }
}
