---
name: prior-audit-decisions
description: Conditions and verdicts issued per wave, so later waves can be checked against unmet conditions.
metadata:
  type: project
---

Running log of verdicts/conditions I have issued. Check new waves against any OPEN conditions.

## Phase-2 — Hidden-Care-Partner conversion nudge (run_partner_nudge_sweep) — VERDICT: PASS WITH CONDITIONS
Scope: `database/09_chat_identity.sql` (new SECURITY DEFINER fn), `BookingMaintenanceWorker.cs`, `WhatsAppAbstractions.cs` (IPartnerNudgeStore), `WhatsAppRepositories.cs` (PartnerNudgeStore), `DependencyInjection.cs`, new `HiddenPartnerNudgeTests.cs`.
Tenant isolation, DEFINER hygiene, PHI (generic text, no patient identity), anti-spam (cooldown + ≥3 threshold), broker exclusion: all PASS.
OPEN CONDITIONS before any PRODUCTION promotional send (the real Meta path) is enabled:
1. (HIGH) Proactive marketing must go via a Meta PRE-APPROVED TEMPLATE, not free-form text. MetaWhatsAppSender currently sends type:"text" only with no per-intent branching. Until a template-send path keyed by message_intent='partner_nudge' exists, the nudge MUST NOT ship to the real Meta sender. See [[outbox-whatsapp-send-path]].
2. (HIGH) DPDP lawful basis: record/track promotional-outbound opt-in (or documented legitimate-use basis) per recipient, and honor a STOP/opt-out keyword that writes consent_revoked and permanently suppresses nudges. No promotional consent class exists today. See [[consent-dpdp-tables]].
3. (MEDIUM) Outbox scrub: mark_outbox_sent/mark_outbox_failed redact only consent_otp/claim_otp bodies. partner_nudge body is non-secret marketing text, so no redaction needed — but confirm no PHI ever enters the text (today it's clinic display_name only — OK).
These are PASS-WITH-CONDITIONS items: the DEV/stub path (logs only) is safe to merge now; the conditions gate enabling the live Meta promotional send.

## Phase-2 — Payout EXECUTE gateway-outside-txn + idempotency (ExecutePayoutCommandHandler) — VERDICT: PASS WITH CONDITIONS
Scope: `mediq.Application/Cqrs/Behaviors.cs` (new `ISelfManagedTransaction` marker — ONLY ExecutePayoutCommand implements it; UoW behavior skips its ambient tx), `mediq.Application/Features/Commission/PayoutFeatures.cs` (3-phase execute), `mediq.Infrastructure/Commission/CommissionRepositories.cs` (MarkPaid/MarkFailed now return bool via conditional `WHERE status='processing'`), `PayoutGateways.cs` (deterministic DRYRUN ref), `CommissionAbstractions.cs` (PayoutInstruction.IdempotencyKey).
PASS items verified:
- approval≠execution still distinct keys: `commission.payouts.approve` vs `commission.payouts.execute` (CommissionController.cs ~L154/L164).
- RLS preserved per phase: each phase opens its own `BeginTenantScopeAsync` (SET LOCAL app.tenant_id) — including the exception-audit scope. UoW skip does NOT drop RLS.
- single-winner gate sound: Phase-3 wallet/attribution side effects run ONLY inside `if (await MarkPaidAsync(...))` (conditional UPDATE matched one row). Concurrent execute/resume that loses → 0 rows → skips side effects. No double-credit under the interleavings walked.
- 'paid' replay path returns recorded reference, no gateway re-call, no re-credit. Correct.
- leaving 'processing' on gateway EXCEPTION (not 'failed') is the right call — marking failed would release for a fresh disbursement → double-pay. Audited.
- integration event `commission.payout.paid` carries IDs + net_inr only, NO PHI. Audit rows written for every transition (claim implicit, paid, failed, ambiguous-exception).
- deterministic DRYRUN ref is fine: `payment_reference` is NOT unique in schema (07_commission_broker.sql L432; only invoice_number L436 is). `payment_gateway` col exists (L433). No collision risk, no info leak (dry-run only, label says DRY RUN).
OPEN CONDITIONS (pre-prod, before the LIVE gateway adapter is wired — Stub never throws so none bite today):
1. (HIGH, pre-prod) STUCK-'processing' RECONCILIATION GAP: an ambiguous gateway exception leaves the payout 'processing' forever unless a human/worker re-executes. No reconciliation worker, no alert, no `processing`-age monitor exists. Before live go-live: add a reconciler that queries the gateway by IdempotencyKey for aged-'processing' payouts and finalizes, + an alert. Track as a live-gateway go-live gate.
2. (HIGH, pre-prod) LIVE ADAPTER MUST FORWARD IdempotencyKey as the gateway dedupe field (RazorpayX idempotency header / Cashfree transferId). The entire no-double-pay guarantee on resume depends on this. The Stub models it; the real adapter MUST honor it. Gate live go-live on an adapter test proving same-key → same transfer.
See [[payout-finalize-side-effect-scoping]] for the MEDIUM cross-batch concern (pre-existing, not introduced here).

## Phase-4 — Durable async webhook delivery (WebhookDeliveryWorker / WebhookDeliveryDrainStore) — VERDICT: PASS WITH CONDITIONS
Scope: WebhookPublisher (enqueue-only), new WebhookDeliveryDrainStore + WebhookDeliveryWorker + WebhookDeliveryOptions, IWebhookDeliveryDrainStore/ClaimedWebhookDelivery, DI, Program.cs, test host + 2 new tests. NO schema/RBAC/PHI/payout/encryption change — confirmed. See [[webhook-delivery-drain]].
All security gates PASS: no RLS bypass (webhook tables not RLS-protected — verified no ENABLE RLS on platform_api.* anywhere; app role has direct grants), secret stays encrypted (registered `05_security_hardening.sql:156`, SignWithProtected decrypts locally + returns digest only), no PHI in payload/logs, audit chain untouched, no secrets in code/logs, no new permissions, webhook gate N/A (this is OUTBOUND delivery, not the inbound Meta WhatsApp webhook — Meta signature verification gate does not apply here).
CONDITIONS — STATUS (re-verified 2026-06-29, FINAL VERDICT now PASS):
1. (MEDIUM) [CLOSED + re-verified] MarkFailedAsync/MarkDeliveredAsync 2nd statement (subscription health) was unconditional on webhook_id. FIXED: both methods now gate the health UPDATE via `WITH won AS (UPDATE webhook_deliveries ... WHERE delivery_id=@p0 AND status='processing' RETURNING webhook_id) UPDATE webhook_subscriptions s ... FROM won WHERE s.webhook_id=won.webhook_id` — stale loser → won empty → health UPDATE 0 rows. Regression test `Webhook_Stale_SingleWinner_Loser_Does_Not_Perturb_Subscription_Health` (PlatformApiTests.cs:190) delivers success then calls MarkFailedAsync on the now-'success' row, asserts status stays 'success' AND consecutive_failures stays 0 (would bump to 1 without the gate — genuine guard). Independently ran: build 0 err, PlatformApiTests 8/8, full suite 208/208.
2. (INFO/LOW) [CLOSED 2026-06-29 by feat/phase4-hardening-sweep] ClaimDueAsync claims status IN ('pending','failed','processing') with elapsed next_retry_at, but partial index idx_webhook_deliveries_pending covered only ('pending','failed'). FIXED: predicate broadened to ('pending','failed','processing') in `02_platform_api.sql:261-262` + bundle `docslot_complete.sql:1151-1152`; live `docslot_platform` reindexed (pg_indexes confirms). Now byte-identical to the cleared idx_integration_outbox_due (`02_platform_api.sql:354-355`). Stranded-'processing' reclaim is index-covered. Pure additive index coverage — no column/CHECK/RLS/GRANT change.
3. (INFO, pre-existing) [CLOSED 2026-06-29 by feat/phase4-hardening-sweep] `mediq.AppHost` dragged in MessagePack 2.5.192 — NU1903 (GHSA-hv8m-jj95-wg3x). FIXED: direct `<PackageReference Include="MessagePack" Version="3.1.7" />` pin in `mediq.AppHost/mediq.AppHost.csproj` overrides the transitive vulnerable version (Aspire.Hosting.PostgreSQL + .RabbitMQ 13.4.3). No first-party MessagePack usage (grep clean across *.cs), AppHost orchestration-host only — no data/auth/PHI path. `dotnet list … --vulnerable --include-transitive` reports ZERO vulnerable packages across all 10 projects. Comment warns the pin must outlive Aspire 13.4.6 (still resolves vulnerable 2.5.302).

## Phase-4 — YARP edge gateway hardened to ENFORCED trust boundary (mediq.Gateway) — VERDICT: PASS (2026-06-29)
Scope: `mediq.Gateway/appsettings.json` (6 anon allow-list routes Order=1 pinning Path+Methods + AuthorizationPolicy "anonymous"; catch-all `platform-api` `/api/v1/{**catch-all}` now AuthorizationPolicy "default" + Order=100), `mediq.Gateway/Program.cs` (request transform remints X-Correlation-ID Remove+Add fresh Guid + strips X-Tenant-Id; response X-Content-Type-Options:nosniff global, Cache-Control:no-store scoped to /auth/login,/auth/refresh,/oauth/token; exposes `public partial class Program;`), test files GatewayWebAppFactory.cs + GatewayTrustBoundaryTests.cs + GatewayCollection.cs (all UNTRACKED `??`), csproj ProjectReference Aliases="Gateway". NO schema/RBAC/PHI/payout/encryption change — confirmed.
Established facts (verified, reusable):
- YARP 2.3.0 reserved AuthorizationPolicy keywords are CASE-INSENSITIVE: "default"→app default policy (= RequireAuthenticatedUser from bare `AddAuthorization()` with no DefaultPolicy/FallbackPolicy override), "anonymous"→disables edge authz for that route. Confirmed from Yarp.ReverseProxy.xml lib doc.
- Edge JWT validation UNCHANGED: issuer docslot-platform, audience docslot-clients, 30s ClockSkew, ValidateLifetime. User-login + OAuth client tokens minted identically (same iss/aud/key) so single-audience edge check accepts both. No second audience added — no trust-surface widening.
- DEFENSE IN DEPTH intact: API (`mediq.Api/Program.cs`) independently runs UseAuthentication/UseScopeResolution/UsePermissionResolution/UseAuthorization + WhatsApp HMAC; gateway is ADDITIVE. Hitting API directly is no worse than before.
- Tenant: API derives tenant ONLY from JWT `tenant_id` claim or server-side ITenantScopeOverride (WhatsApp phone_number_id map). CurrentUserContext has NO X-Tenant-Id fallback (`mediq.Api/Context/RequestContext.cs:43`). Stripping X-Tenant-Id at edge is zero-risk DiD.
- Correlation: API CorrelationIdMiddleware HONORS inbound X-Correlation-ID; gateway remint (Remove+Add) makes the edge the sole trustworthy source. Correlation is tracing-only (HttpContext.Items), never authz/tenant — remint can't break a flow.
- Anon allow-list EXACTLY matches API `[AllowAnonymous]` set: auth/login(POST), auth/refresh(POST), oauth/token(POST), oauth/revoke(POST), whatsapp/webhook(GET+POST), ref/{shortCode}(GET). No protected endpoint exposed by an anon route; no genuinely-anon flow locked out.
- ADVERSARIAL PROBE (I wrote a temp test, ran 12 cases, then DELETED it — not committed): dot-segment traversal (`/auth/login/../bookings`), encoded-slash (`..%2f`), method-confusion (DELETE/PUT/GET/POST on pinned anon routes → fall to authed catch-all → 401, stub never receives), case variation, trailing slash, protected-path case variation → ALL safe. Pass-through headers Authorization/X-Hub-Signature-256/Idempotency-Key/X-Purpose-Of-Use preserved unmodified. WhatsApp webhook reaches stub w/o token, signature intact.
- Tests: 22 gateway tests pass; full suite 235/235 after fresh build (212 baseline + 23 gateway-related). NOTE: the 3 gateway TEST files are git-UNTRACKED — they MUST be `git add`ed or the boundary ships testless.
- Committed dev SigningKey in gateway appsettings is PRE-EXISTING (identical to mediq.Api appsettings, line unchanged by this diff, labelled dev-only-replace-in-production). Pre-existing exposure, NOT introduced here; prod must source from env/GHA secret. Same posture as API.
CONDITIONS (non-blocking, PASS):
1. (LOW) `git add` the 3 untracked gateway test files (GatewayWebAppFactory.cs, GatewayTrustBoundaryTests.cs, GatewayCollection.cs) before commit — else the trust boundary merges with zero tests.
2. (INFO, pre-prod) Replace the dev `Jwt:SigningKey` in BOTH mediq.Gateway and mediq.Api appsettings with an env/GHA-secret-sourced value in production. Pre-existing platform-wide condition, not gateway-specific.

## Phase-4 — Auth-edge hardening: JWT signing-key fail-fast guard + XFF-aware per-IP rate limiting — VERDICT: PASS (2026-06-30)
Scope (working tree, UNCOMMITTED at audit; branch `feat/phase4-auth-edge-hardening` had ZERO commits past main 2f966c9 — same uncommitted-working-tree pattern as the retention-pruner wave): NEW `mediq.ServiceDefaults/JwtSigningKeyGuard.cs` + `ForwardedHeadersConfig.cs`; modified `mediq.Api/Program.cs`, `mediq.Gateway/Program.cs`, `mediq.Api/appsettings.json`, `mediq.Gateway/appsettings.json`, `mediq.AppHost/AppHost.cs` (commented seam only), `mediq.Api/Extensions/ApiServiceExtensions.cs` (comment-only — TODO reworded), `tests/.../TestHostConfig.cs`, `tests/.../GatewayWebAppFactory.cs` (sealed→unsealed); NEW tests `AuthEdgeHardeningUnitTests.cs` (12) + `GatewayAuthEdgeTests.cs` (2). Touches the JWT trust boundary + edge rate limiter — squarely in the gate.
GATE RESULTS — all PASS:
- Guard fails CLOSED: missing key / non-base64 / dev-key-outside-Dev (base64 EQUALITY against `ZGV2...biE=`) / <32-byte key → throws InvalidOperationException, host refuses to boot. Verified the LENGTH gate alone would PASS the dev key (decodes 51 bytes ≥32) — the blocklist EQUALITY is what catches it; BOTH checks present (`JwtSigningKeyGuard.cs:56,58,68`). Called right after `builder.Build()` in BOTH Programs (`mediq.Api/Program.cs:96`, `mediq.Gateway/Program.cs:71`).
- NO weakening of existing auth: JWT validation params (issuer docslot-platform / audience docslot-clients / ValidateIssuerSigningKey / ValidateLifetime / 30s ClockSkew) in `ApiServiceExtensions.AddPlatformJwtAuth` UNCHANGED — diff to that file is comment-only. Guard is purely ADDITIVE enforcement.
- Test exemption is SOUND, not a hole: `TestHostConfig` [ModuleInitializer] sets ENV `Jwt__AllowDevSigningKey=true` suite-wide so every test host accepts the dev key regardless of env; `GatewayAuthEdgeTests` overrides it back to false (+ `UseEnvironment("Production")`) via AddInMemoryCollection layered AFTER AddEnvironmentVariables → in-memory wins for THAT host only, proving the throw. Production default for `Jwt:AllowDevSigningKey` is the env default (false outside Dev) — exemption only affects test hosts, never prod.
- XFF default-deny is AIRTIGHT against spoofing (the crux): builder CLEARS `KnownIPNetworks` + `KnownProxies`, adds only configured entries; committed appsettings ship BOTH arrays empty → 0 networks + 0 proxies → XFF ignored → limiter keeps the raw socket IP (cannot spoof the rate-limit partition). DECISIVELY verified the cleared collection is the consulted one: .NET 10 framework doc (`Microsoft.AspNetCore.App.Ref/10.0.3/.../Microsoft.AspNetCore.HttpOverrides.xml`) states the internal list keeps `System.Net.IPNetwork` (KnownIPNetworks) and the OBSOLETE `Microsoft.AspNetCore.HttpOverrides.IPNetwork` (KnownNetworks) collections IN SYNC — "Modifications through either interface are reflected in the other." So clearing KnownIPNetworks empties BOTH views; the live-vs-legacy distinction is MOOT — no loopback-trust leak possible. KnownNetworks is marked Obsolete→use KnownIPNetworks. Only XForwardedFor honored (not Proto). Invalid entries TryParse-skipped (typo can't crash boot).
- api per-CLIENT limiter (PR#33) UNTOUCHED: it lives in `ApiClientRequestLogMiddleware` (UseApiClientRequestLog), partitions on clientId not IP — independent of XFF. The two per-IP GlobalLimiters (gateway/api) partition on `ctx.Connection.RemoteIpAddress` — diff added only comments + the 2 new statements, partition logic unchanged. `UseForwardedHeaders` precedes `UseRateLimiter` in BOTH (api 105<113, gw 76<78).
- AppHost change is comment-only seam (commented AddParameter + commented WithEnvironment) — zero runtime behavior change confirmed.
- Honest-limitation claim is HONEST: end-to-end XFF partitioning not integration-testable (WAF TestServer synthesizes RemoteIpAddress=127.0.0.1, bypasses ForwardedHeaders) → covered by unit tests on the builder (default-deny both-empty / parsing / invalid-skip) + the no-regression `GatewayRateLimitTests` (`GatewayTrustBoundaryTests.cs:239`, asserts 429 on loopback socket IP). Faithful fallback, not omitted.
- Other gates N/A: no schema/RBAC/PHI/payout/encryption/audit-chain change; no new permission; no secret added (the committed dev key is PRE-EXISTING, now GUARDED against prod use — net security IMPROVEMENT); inbound-Meta-webhook gate untouched.
- Independently RE-RAN: `dotnet build mediq.sln` 0 warn/0 err; new auth-edge tests 14/14; FULL suite 273/273 (no flake this run, incl. the known PlatformApiTests webhook concurrency flake which passed).
CONDITIONS (non-blocking, PASS):
1. (LOW) The 4 new files are git-UNTRACKED (`?? JwtSigningKeyGuard.cs, ForwardedHeadersConfig.cs, AuthEdgeHardeningUnitTests.cs, GatewayAuthEdgeTests.cs`) — `git add` them before commit or the guard + its tests ship missing. (Same untracked-files pattern as the gateway and retention-pruner waves.)
2. (INFO, pre-prod) The deferred RS256/JWKS migration + real secret store remains an OPEN platform condition (carried from the gateway wave). This guard is the interim enforcement; it does NOT discharge that condition — it ENFORCES the existing "replace the dev key in prod" condition (was advisory, now fail-closed). The AppHost secret-injection seam must be UNCOMMENTED + populated (`Jwt__SigningKey` on both api+gateway from a real secret) for prod, else the guard correctly refuses to boot.

## Phase-4 — Retention pruner for the two append-only platform_api operational tables — VERDICT: PASS (2026-06-30)
Scope (working tree, UNCOMMITTED at audit time — branch had only a memory-doc commit on top of main): new `RetentionPruneStore.cs`, `RetentionPruneWorker.cs`, `RetentionPruneOptions.cs`, `RetentionAbstractions.cs (IRetentionPruneStore)`, `RetentionPruneTests.cs` + `IntegrationOutboxCollection.cs` (untracked); modified `02_platform_api.sql` (2 partial retention indexes + outbox comment), `10_roles_grants.sql` (+1 GRANT DELETE line: integration_event_outbox), `docslot_complete.sql` (bundle regenerated in lockstep), `Program.cs`, `DependencyInjection.cs`, `appsettings.json`, `TestHostConfig.cs`. NEW DELETE privilege + PHYSICAL row deletion — squarely in the gate.
PHYSICAL-DELETE-vs-soft-delete RULING (the crux): SOUND. The CLAUDE.md "soft-delete everywhere / never DELETE" rule binds `deleted_at`-bearing DOMAIN tables and the hash-chained `audit_log`. Neither target has `deleted_at` (verified in DDL) — both are transient operational queue/delivery logs. The GRANT DELETE block's own comment (`10_roles_grants.sql:59-61`) already sanctions exactly this class ("transient tables legitimately need DELETE … idempotency pruning … NEVER on audit_log/audit_chain"); `webhook_deliveries` was ALREADY in that block, this only ADDS `integration_event_outbox`. audit_log/audit_chain REVOKE DELETE + block trigger are adjacent and UNTOUCHED by the diff (only "audit" text in the diff is the explanatory comment). payload is IDs/tokens ONLY (DDL comment + PR#38 memo) — prune destroys no PHI and nothing a DPDP/erasure flow depends on.
SAFETY INVARIANT (no data-loss): VERIFIED, double-guarded. Both DELETEs are `WHERE status='success' AND <published_at|delivered_at> IS NOT NULL AND <ts> < cutoff`, batched subselect+LIMIT looped to maxBatches (bounded lock). The IS NOT NULL guard is REAL, not theater: I traced every writer of the two completion timestamps — `published_at` set ONLY by `IntegrationEventOutboxDrainStore.MarkPublishedAsync` atomically with status='success'; `delivered_at` set ONLY by `WebhookDeliveryDrainStore.MarkDeliveredAsync` AND `WebhookRepositories.MarkSuccessAsync`, both atomically with status='success'. ALL FOUR MarkFailed variants NEVER touch the completion ts → a failed/abandoned row keeps it NULL. status CHECK = ('pending','processing','success','failed','abandoned') only → no other terminal value can carry a non-null completion ts. So a 'failed' (retryable) row is doubly ineligible. Test `RetentionPruneTests.cs` seeds old-success/recent-success/failed(nextRetryAt+attemptCount, NULL published_at)/abandoned/pending with UNIQUE event_ids and asserts ONLY old-success pruned, other 4 survive — asserting on its OWN ids (not global counts; shared hot table). The failed-survives assertion is a genuine proof.
Other gates: tenant N/A (non-RLS PaaS log by documented convention — confirmed NO `ENABLE ROW LEVEL SECURITY` on either table anywhere; global cross-tenant prune is housekeeping on a non-RLS log, not a disclosure path); no SECURITY DEFINER (correct — plain app-role SQL, mirrors drains); role is NOSUPERUSER/NOBYPASSRLS so prune cannot escape RLS on any table that did have it. DEFAULT-OFF: `Retention:PrunerEnabled=false` in appsettings, Program.cs gate registers worker only when true, TestHostConfig force-OFF. No new permission, no secret, no encryption change, audit chain untouched, webhook(inbound-Meta) gate N/A. Bundle grant block byte-identical to source; both `idx_*_retention` partial indexes (`WHERE status='success'`) present in source + bundle.
CONDITIONS (non-blocking):
1. (LOW) The new test files `RetentionPruneTests.cs` + `IntegrationOutboxCollection.cs` are git-UNTRACKED — `git add` them before commit or the pruner ships testless. (Same untracked-test pattern as the gateway wave.)
