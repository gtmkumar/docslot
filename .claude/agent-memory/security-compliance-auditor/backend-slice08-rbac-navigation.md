---
name: backend-slice08-rbac-navigation
description: Slice 08 rbac_navigation — super_admin universal sweep + new keys + menu seeding; cleared PASS; doctor tenant-scope is pre-existing README/seed drift
metadata:
  type: project
---

`backend/mediq/` Slice 08 (rbac_navigation: permission completeness + menu seeding). Audited 2026-06-14 — PASS, no blockers. Build clean, 41/41 tests pass. (Schema integrity pre-verified by maintainer: 115 CREATE TABLE in bundle, 115 live base tables — the "131" report was a miscount, not a change.)

**LIVE-VERIFIED (built the role-permission matrix from role_permissions/roles/permissions):**
- GATE 1 super_admin sweep: holds 127/127 permissions. Sweep at END of `10_roles_grants.sql:142` (terminal file; runs after 04_future_products which inserts perms after 08 in bundle order ...08,09,04,10 — placement reasoning correct, a sweep in 08 would miss 04's keys). `CROSS JOIN platform.permissions WHERE role_key='super_admin' AND is_system=true ON CONFLICT DO NOTHING` — targets ONLY super_admin, so it can't leak keys to other roles. Self-certifying: `RAISE EXCEPTION` if v_have<>v_total. super_admin correctly holds the dangerous keys (break_glass, payouts.execute, broker.blacklist, deletion.certify, tenants.delete, audit.anchor).
- GATE 2 over-grant (the important one) — NO escalation introduced: `commission.payouts.execute` = super_admin ONLY (SoD intact, tenant_admin does NOT hold it); `commission.broker.blacklist` = super_admin only; deletion.certify/audit.anchor/tenants.delete = super_admin only. broker role = ONLY read_self/update_self/generate_link_self (self scope). tenant_viewer = ZERO non-read actions (read-only confirmed). platform_support holds breach.read + users.impersonate (matches README). NOTE (not a slice-08 blocker): doctor holds tenant-scoped docslot.booking.complete + .no_show + medical_access.break_glass + several ai.* keys — README scopes doctor to "own bookings + patient read." But booking.complete was granted to doctor in the ORIGINAL 03_docslot.sql:873 and break_glass/ai.* in 06; slice-08's no_show inheritance correctly followed booking.complete. So this is PRE-EXISTING README-vs-seed drift, not a slice-08 over-grant. Flag for a future scope-tightening review if doctor should be own-bookings-only; not introduced here.
- GATE 3 new keys + inheritance: docslot.patient.create (dangerous) + docslot.booking.no_show added (`08:581,583`). Inheritance verified CLEAN live: patient.create granted to EXACTLY the roles holding patient.update (0 over-grant); no_show to EXACTLY the roles holding booking.complete (0 over-grant). Endpoints re-gated: PatientsController POST→patient.create (`:51`), BookingsController no-show→booking.no_show (`:84`), complete→booking.complete (`:90`); RequirePermission fails closed so callers without the new key are denied; inheritance prevents locking out legitimate holders.
- GATE 4 menu gates: 21 active navigation_menus, ALL bilingual (menu_label_hi present). Privileged menus gated: security→platform.audit.read, developers→platform.api_clients.manage, care_partners*→commission.broker.read/rules.read, team/settings.users→tenant.roles.assign. 3 ungated/world: dashboard, settings (group header), settings.tenant (Organization — mild, tenant-config view not privileged data). Live simulation of doctor's perm set: care_partners/security/developers/team/analytics/lab/doctors all HIDDEN; doctor sees bookings+patients+dashboard (~5 leaf menus). Low-priv role cannot see privileged menus. get_user_menus filters by permission AND tenant_type.
- GATE 5 medical_access.* product_id NULL: COSMETIC. The sweep is product-agnostic (no product filter) so super_admin holds both medical_access keys despite NULL; authz/menu gates key off permission_key not product_id. Per-schema reporting only, no functional authz impact.
- GATE 6 no regression: RLS still on 5 PHI tables; trg_audit_log_append_only present; 0 MediatR refs (custom CQRS intact). Slice-07 fixes (SET LOCAL tenant scope, broker IDOR) untouched.

**Slice-08 closes the super_admin RBAC hole flagged in slice 07.** Remaining carryover (unchanged, not blocking): KMS/RS256-JWKS, register webhook AES field, webhook replay/SSRF, MessagePack bump, access_policies column gate, dedicated table-owner/FORCE RLS. NEW tracked observation: revisit whether `doctor` should hold tenant-wide booking.complete/no_show/break_glass vs own-bookings-only (README/seed drift, pre-dates slice 08).
