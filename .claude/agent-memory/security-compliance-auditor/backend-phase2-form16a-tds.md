---
name: backend-phase2-form16a-tds
description: Phase-2 Form 16A / TDS certificate + payout invoice numbering — FINAL PASS (HIGH idempotency-cache-of-full-PAN leak closed via IDoNotCacheResponse marker + regression test)
metadata:
  type: project
---

Phase-2 commission change: payout invoice numbering + Form 16A (TDS u/s 194H) certificates. Audited 2026-06-29. FINAL VERDICT: PASS (the one HIGH finding was remediated + re-verified same session). Files: `database/07_commission_broker.sql` (tds_certificates table C8b + invoice trigger + perm), `database/01_platform_core.sql` (tenants.tan), `Form16AFeatures.cs`, `TdsCertificateRepository.cs`, `HtmlForm16ADocumentRenderer.cs`.

**VERIFIED CORRECT (don't re-derive):**
- INVOICE TRIGGER `commission.generate_payout_invoice_number` (07:468) is a faithful mirror of `docslot.generate_booking_number` (03:924): BEFORE INSERT FOR EACH ROW, `IF NEW.invoice_number IS NULL` guard, global `commission.invoice_number_seq`, `INV-YYYYMM-NNNNN`. No user input (nextval/TO_CHAR(NOW())) → no injection. Runs for EVERY payout INSERT regardless of code path. Global non-resetting seq satisfies `payouts.invoice_number VARCHAR(50) UNIQUE` (07:436). Acceptable.
- `commission.tds_certificates` (07:498): tenant_id NOT NULL FK→tenants; RLS predicate `tenant_id = current_tenant_id() OR = current_impersonated_tenant()` IDENTICAL to all other commission tables (07:989-1000). Stores deductee_pan_last4 VARCHAR(4) ONLY — full PAN never in a column. UNIQUE(payout_id). status CHECK in (provisional/filed/issued/revised), default provisional. quarter CHECK in Q1..Q4.
- PERMISSION `commission.tds.issue` (07:889): scope=tenant, is_dangerous=TRUE. Granted by-pattern (not explicit list): super_admin (all commission), tenant_owner (scope tenant/self), tenant_admin (scope=tenant minus execute/blacklist) → all GET it. tenant_staff allow-list (07:935-939) does NOT include it → 403. Gating the full-PAN document GET behind this DANGEROUS perm is MORE protective than a soft read perm — correct.
- PAN AT REST: `commission.brokers.pan_number` registered in encrypted_fields_registry (07:970) as data_class=tax_id, pii_category=financial, legal_basis=legal_obligation. Satisfies DPDP hard constraint. Handler decrypts transiently via `IFieldEncryptionService.DecryptAsync(BrokerFields.Pan,…, EncryptionContext(userId,tenantId,"tds_certificate",payoutId,ip))`; last4 = `pan[^4..]` (safe for short strings; PAN validated 10-char on register). Full PAN appears only in the rendered HTML (transient). NO plaintext PAN in audit ChangeSummary (only invoice#/FY/Qtr/amounts).
- COMMAND-NOT-QUERY justification is SOUND + verified: `FieldEncryptionService.LogUsageAsync` writes key_usage_log via `db.Database.ExecuteSqlRawAsync` on the AMBIENT PlatformDbContext tx. Command pipeline = `UnitOfWorkBehavior` → BeginTenantScope → handler → SaveChanges → **CommitAsync** (COMMITS). Query pipeline = `TenantScopeQueryBehavior` → scope → dispose WITHOUT commit (**ROLLS BACK**). So a query-shaped document read would silently discard the key_usage_log decrypt entry. Making GetForm16ADocument a COMMAND is the correct fix for that. (Behaviors.cs:49-57 vs 176-194.)
- TENANT ISOLATION on the source join: `TdsCertificateRepository.GetSourceAsync` joins payouts+brokers+platform.tenants (tenants has NO RLS) but WHERE `p.payout_id=@p0 AND p.tenant_id=@p1` — payout row is RLS-scoped AND explicitly tenant-filtered; broker/tenant reached only via that payout's FKs → cannot pull another tenant's PAN.
- TAN: `platform.tenants.tan VARCHAR(10)` next to pan/gstin — correct platform placement (regulatory deductor identity, not product-specific). Nullable → renderer prints "not configured" and cert stays provisional. Honest.
- HONESTY: status ALWAYS 'provisional' on issue, traces_certificate_number ALWAYS null (never fabricated). Renderer marks PROVISIONAL banner unless status issued/filed. PDF + TRACES filing correctly external-gated, not faked.

**HIGH — CLOSED (remediated + re-verified 2026-06-29).** FIX (option a): new marker `IDoNotCacheResponse` (Behaviors.cs:175); `IdempotencyBehavior.Handle` short-circuits `if (request is IDoNotCacheResponse) return await next();` at the very top (Behaviors.cs:132 — no TryGet/serve, no SaveAsync, even with a header present). `GetForm16ADocumentCommand` implements it (Form16AFeatures.cs:77). CONDITION-2 (key_usage_log still commits) HOLDS — verified the registration order INDEPENDENTLY (not on coordinator's word): DependencyInjection.cs:31-32 registers Idempotency THEN UnitOfWork; CommandDispatcher reverses+folds (Dispatchers.cs:20-28) → execution nesting Logging→Validation→Idempotency→UnitOfWork→DomainException→handler, so Idempotency wraps OUTSIDE UnitOfWork; the bypass still descends into UnitOfWorkBehavior's committed tenant-scoped tx → the decrypt's key_usage_log INSERT commits. NO second sink: GetForm16ADocumentCommandHandler does ONLY the transient decrypt (no audit/upsert/store of the body — the audit/upsert at Form16AFeatures.cs:51-61 are in the ISSUE handler); controller streams via `Content(doc.Content)` (CommissionController.cs:190), no caching. Regression test `Form16A_Document_WithIdempotencyKeyHeader_NeverPersistsFullPanToIdempotencyStore` (CommissionPipelineTests.cs:680) GETs WITH a unique Idempotency-Key header, asserts full PAN served AND `idempotency_keys.response_payload LIKE '%PAN%'` count==0. Suite 22/22.
GENERAL LESSON: the command pipeline's IdempotencyBehavior caches command RESPONSES to plaintext `idempotency_keys`. Any command whose response carries sensitive rendered content (full PAN/PHI/secrets) MUST implement `IDoNotCacheResponse` (or be a query). Check this on every future command that returns decrypted/rendered sensitive payloads.
ORIGINAL DEFECT (context): `GetForm16ADocumentCommand` is dispatched on the COMMAND pipeline (`commands.Send`), which includes `IdempotencyBehavior`. That behavior keys off the `Idempotency-Key` HTTP header (header always wins, IdempotencyContext.Key, even on a GET). The command does NOT implement IRequireIdempotency, so a MISSING key is fine (falls through). BUT if any caller sends an Idempotency-Key header on the document GET, the behavior does `store.SaveAsync(… JsonSerializer.Serialize(response) …)` where response=Form16ADocumentResult.Content = the FULL-PAN HTML. `idempotency_keys.response_payload` is plaintext TEXT (01:739), NOT in encrypted_fields_registry, retained ~7 days, and NOT reachable by crypto-erasure (key destruction doesn't touch it) → defeats the change's own "full PAN never persisted at rest" guarantee. Not exploitable by the CURRENT SPA (frontend idempotency seam attaches the header only when req.idempotency set, not on GETs) → not a merge-blocker on synthetic data, but MUST fix before real broker PANs land. Remediations (any): (a) exclude full-PAN/document command-results from idempotency capture (skip SaveAsync when handler returns a document/raw-content result, or add an IDoNotCacheResponse marker the behavior honors); (b) make the document read a QUERY and move the key_usage_log write onto its OWN committed connection/tx (decouples "log must commit" from "command pipeline"); (c) strip the header for GET in IdempotencyContext. Prefer (a)+(b).

**LOWER FOLLOW-UPS:** none material. last-4 derivation safe; bundle/source in parity (tan + tds_certificates + perm in both 07 and docslot_complete); audit-chain append-only untouched; no purpose_of_use_log needed (PAN is tax-id PII not medical PHI — key_usage_log resource_type=tds_certificate is the right forensic record; the docslot PHI purpose-of-use gate applies to medical tables only).
