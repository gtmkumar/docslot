# dotnet-microservices-architect — Memory Index

- [Slice 95 people import/export](project_slice95_people_import_export.md) — People CSV export (reuse AuditCsv neutralization) + bulk import; per-row SAVEPOINT inside command UoW un-aborts R3-escalation-42501 rows so batch continues; tenant from JWT.

- [Slice 89 invitations](project_slice89_invitations.md) — token-based invitations (SHA-256 hash, unauth accept via definer fn). GOTCHAs: text→citext overload fails (declare TEXT params); OUT-name/ON CONFLICT column collision (42702, out_-prefix); audit of uncommitted new user needs NULL actor (dedicated-conn FK).

- [Dashboard API Contracts (Wave 2)](project_dashboard_contracts.md) — DocSlot dashboard DTO/enum contracts in mediq.SharedDataModel, what they mirror in canonical SQL, envelope/timezone/PHI/idempotency conventions.
- [Slice 01 platform_core](project_slice01_platform_core.md) — backend topology, custom CQRS shape, EF database-first (inet converter gotcha), resolve-once RBAC, bcrypt+argon2 auth, audit-chain trigger, Swashbuckle-incompatible-with-net10.
- [Slice 02 platform_api](project_slice02_platform_api.md) — OAuth client-credentials, dual auth (perm vs scope via token_use claim), webhook HMAC+Polly retry+outbox, secret-at-rest rules (webhook secret AES-encrypted not hashed).
- [Slice 03 docslot](project_slice03_docslot.md) — booking aggregate/state-machine (6 states, trigger-assigned number+history), app-owned slot_holds+idempotency_keys tables, durable idempotency, booking integration events, PHI masking + purpose-of-use consent gate; clinical PHI deferred to 03b/05.
- [Slice 05 security_hardening](project_slice05_security_hardening.md) — FIRST canonical SQL edits (idempotency_keys+slot_holds promoted, audit append-only guard trigger, webhook secret registered) + bundle regen/revalidate; envelope field encryption (KMS abstraction, key_usage log); DPDP crypto-erasure/export/breach/consent; audit-chain verify+anchor; break-glass. GOTCHA: audit chain trigger not concurrency-safe → FIXED in 03b.
- [Slice 03b clinical_phi](project_slice03b_clinical_phi.md) — least-privilege docslot_app role (RLS now ENFORCED, app conn-string switched), audit-chain advisory-lock concurrency fix (parallel tests re-enabled), clinical PHI (prescriptions/labs/history/ABDM) field-encrypted + RLS + consent + purpose-of-use. GOTCHAs: jsonb-encrypted cols need to_jsonb/#>> + direct NpgsqlDataReader (EF record-map FormatException); reads need TenantScopeQueryBehavior + session-scope set_config for RLS.
- [Slice 07 commission_broker](project_slice07_commission_broker.md) — broker (Care Partner) economy: PCPNDT CHECKs, discount↔attribution exclusivity trigger (SQLSTATE 23514→422), MCI 6.4, PAN-encrypted KYC, attribution engine (3 mechanisms + priority + fraud), TDS/GST payout math, payout approve≠execute (distinct perm keys). Canonical edits: docslot_app commission grants, pan_number VARCHAR→TEXT, super_admin commission-perm backfill.
- [Slice 08 rbac_navigation](project_slice08_rbac_navigation.md) — super_admin UNIVERSAL perm sweep (lives at end of 10_roles_grants, self-verifying, auto-covers future products); 2 new docslot keys (patient.create/booking.no_show) inherited from parent keys + endpoint re-gate (keep old FE keys); full 21-menu backend-driven tree (bilingual, tenant_type-aware, Developers/Security/Care-Partners gates). GOTCHA: resolve_user_permissions filters by tenant scope so platform-wide super_admin does NOT resolve all 127 per-request — assert grant invariant at role_permissions table, not via /me/permissions.
- [Slice 90 branch_scope](project_slice90_branch_scope.md) — issue #90 branch/department membership SCOPE = DISPLAY attribute (mirrors module-licensing), NOT access boundary; branches direct RLS-write (no definer, only such platform table), set_membership_scope definer writes ONLY branch_id/department never role_id; scope-bearing membership = primary DESC/granted ASC/id shared by writer+UserDirectory.
- [Slice 91 security_policy](project_slice91_security_policy.md) — tenant security policy in tenants.settings JSONB (no new table); REAL login (2FA-tier/hours/IP via LoginSecurityPolicyGate)/password-min/receptionist-masking enforcement. GOTCHAs: EF ExecuteSqlRaw parses {..} as placeholders (use ARRAY['security']+jsonb_build_object()); docslot.doctor.read_self DOESN'T EXIST (use update_self); tenant_owner lacks ip_allowlist.manage; TestServer RemoteIpAddress null→X-Test-Ip shim.
- [Slice 08b read_endpoints](project_slice08b_read_endpoints.md) — read-list GETs backing FE mocks (api-requests page, security console 6 GETs, clinical lab/abdm/consent lists + deliver, commission attributions). DTOs match frontend/src/lib/mock/contracts.ts (camelCase, swap-ready). PHI FIX: BrokerDto raw phone→masked. GOTCHAs: untyped-NULL filter needs ::uuid/::timestamptz cast; docslot.patients col is phone_number.
- [Phase 4 integration outbox](project_slice_phase4_integration_outbox.md) — durable integration_event_outbox (TABLE 27, no-RLS platform_api convention) + WebhookPublisher transactional tap closes lost-event gap; flagged RabbitMQ seam (Provider=none default, broker/consumer deferred). Bumped Polly.Core→8.6.6; RabbitMQ.Client v7 async API. Suite 247→257.
- [Shared hot-table test isolation](feedback_shared_hot_table_test_isolation.md) — worker-off outbox tables in the live-DB suite are shared hot tables w/ growing pending backlogs; claim tests must loop-drain until they see their own seeded row, and run twice (empty-table false-pass).
- [audit_log append-only teardown](feedback_audit_log_append_only_teardown.md) — audit_log/audit_chain block UPDATE/DELETE for everyone (owner too) via BEFORE trigger; seed with a unique marker + leave rows behind; audit_log has NO RLS so scope reads with explicit tenant_id predicate.
